How to Run 
if you are using the any IDE then you can directly open the files and click on run button to execute the our python script
If you are running it on any command line then use the command as python filename.py