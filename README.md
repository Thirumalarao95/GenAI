1. here we are working with the modules
3. First we need to create the folder with the name of package then
**Task -1:**
5. Create the match_utils.py python file which contains the add(),substract(),Square()
6. we need to implement adddition and substract and square of the given numbers in the functions and finally returns the result

**Task -2:**
1. Create a string_utils.py python file which contains the 3 user defined function
2. capitalize_words() - this function what ever the texrt/sentemnce we are passing as input to our function that will capitalize the words into capital letters
3. reserve_string() - this function will reversing the string which we are paasing as input
4. word_count() - this function returns the total number of words present in out input

**Task -3:**
   1. Creating the new package named it as shop_package
   2. for this task we need to create a folder with shop_package and in that folder we need to create an empty python file which is named as __init__.py file
   3. we need to create a discount package inside of shop_package as follows
   4. create a python file and named it as discount.py and create a user defined function appply_discount()
   5. which takes the 2 arguments based on that it will returns the discount.
   6. required price and discount percentage based on that only it will calculating the discount and return the result
   7. again create a new function with name as flat_discount() with argument as price and it will substracts 50 from the argument and return the result
   8. Again creating the billing.py python file and cpontains the 2 functions. 1. calculate_total(prices):here we need to calciulate the sum of all the values and returns the sum/total
   9. 2.apply_tax(amount):here we are calculating the 5% tax on the amount values and return the result

**Task -4:**
   1. create a new python file named it as main.py
   2. now we need to import the shop_package with it files
   3. we need to call all the functions inside of packages.
