How to Run:
If you are using the Python IDE's then you can click on the run button 
if you are using the terminal then Type the python filename.py 

Matplotlib Assignment we are plot the Graphs with the help of matploytlib module
we can plot box,bar,pie,hist,mixed etc